

# ============================================================================
# PROFIL: HOME (NEON / POSTGRES)
# ============================================================================
# WICHTIG:
# - "jdbc:postgresql://..."
# - Credentials NICHT in der URL
# - channelBinding erstmal weglassen (macht häufig SCRAM/Pooler-Probleme)

spring.datasource.url=jdbc:postgresql://ep-raspy-bird-agdreweq-pooler.c-2.eu-central-1.aws.neon.tech:5432/neondb?sslmode=require
spring.datasource.username=${PGUSER:__SET_ME__}
spring.datasource.password=${PGPASSWORD:__SET_ME__}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Optional: Dialekt Property kann weg; Hibernate erkennt PostgreSQL selbst
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# KI-Key bitte auch als Env-Var (nicht in Git/properties)
ai.api.key=${OPENROUTER_API_KEY:__SET_ME__}
ai.api.url=https://openrouter.ai/api/v1
ai.model=deepseek/deepseek-chat